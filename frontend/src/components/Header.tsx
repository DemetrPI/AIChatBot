import { AppBar, Toolbar } from "@mui/material";

const Header = () => {
  return (
    <AppBar
      sx={{
        bgcolor: "transparent",
        position: "sticky",
        boxShadow: "lg",

      }}
      
    >
<Toolbar
sx= {{
    display: "flex",
    
}}
>

</Toolbar>

    </AppBar>
  );
};

export default Header;
